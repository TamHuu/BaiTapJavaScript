// Bạn nên sử dụng useState khi bạn cần thay đổi giá trị của một biến trong component 
// và muốn component được render lại để phản ánh giá trị mới. 

// import React, { useState } from 'react';

// function Counter() {
//   const [count, setCount] = useState(0);

//   const incrementCount = () => {
//     setCount(count + 1);
//   }

//   return (
//     <div>
//       <p>Count: {count}</p>
//       <button onClick={incrementCount}>Increment Count</button>
//     </div>
//   );
// }

// export default Counter;
